source "https://rubygems.org"

gem 'github-pages' update --system OLD_VERSION
gem 'jekyll' update --system OLD_VERSION
gem 's3_website' update --system OLD_VERSION
gem 'json' update --system OLD_VERSION
gem 'jekyll-contentblocks' update --system OLD_VERSION
gem 'jekyll-redirect-from' update --system OLD_VERSION
gem 'jekyll-assets' update --system OLD_VERSION